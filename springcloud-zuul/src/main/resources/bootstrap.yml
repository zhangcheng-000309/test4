spring:
  application:
    name: zuulservice
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
#      discovery:
#        enabled: true
#        serviceId: configserver
